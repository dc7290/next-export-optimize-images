{
  "name": "example-common",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "generate": "yarn build && next export",
    "lint": "next lint . --cache"
  },
  "dependencies": {
    "next": "latest",
    "react": "latest",
    "react-dom": "latest",
    "next-export-optimize-images": "latest"
  },
  "devDependencies": {}
}
